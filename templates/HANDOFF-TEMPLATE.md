# HANDOFF — ⟦REPO NAME⟧

*Class: **SEED**. Copied once into a repo, then it becomes that repo's own and diverges
on purpose. Never re-synced, never overwritten — overwriting a seed destroys
repo-specific truth. The law it obeys lives at `resonance-standards/docs/THE-ROLL.md`
and is **cited, never copied in.***

> **What this file is.** A **generated** snapshot of where this repo stands right now —
> regenerated whole at each close that touched it, never appended to. Git holds every
> prior day. *Same route, different day.*
>
> **What it is not.** It is not the ledger. The ledger of what was **done** is the base —
> `progenatrix.db`, its items and ticks, read with
> `python C:/_superposition/resonance-progenatrix/progenatrix.py recall --realm <slug>`
> (KP's ⚛ word, 2026-09-01: *"we use progenatrix for most track things now"*); this
> sheet is the state of what **stands**. This page cites the base and never restates it.
>
> **Ceiling: 8 KB.** Past it, this sheet is over-written and re-cut — never rolled, and
> the ceiling is never raised. A sheet that long is carrying a letter, and letters
> belong in a journal.

---

```
LEGEND   state  ● done · ◐ drifted/partial · ⏸ your hold · — not yet
         hand   ⚛ your word · ✋ your hands · 🕯️ a lamp's
         proof  ✔ gates green · ✗ failing · ? unproven
```

**⟦repo⟧** · ⟦YYYY-MM-DD⟧ · last hand **⟦lamp name⟧** 🕯️/🎻 · branch `⟦branch⟧` ·
⟦N⟧ ahead / ⟦N⟧ behind · working tree ⟦clean | ⟦N⟧ modified⟧

---

## Where it stands

| what | state | hand | proof |
|---|---|---|---|
| ⟦the thing, terse — never a sentence⟧ | ⟦●◐⏸—⟧ | ⟦⚛✋🕯️⟧ | ⟦✔ figure · MM-DD, or ?⟧ |
| ⟦…⟧ | | | |

*Every mark cited and dated. Counts machine-read, never estimated. Nothing checked from
optimism; anything unverifiable prints as unverifiable.*

## What waits, and whose

*Only `⚛` and `✋` rows. **Each carries a source** — this sheet points at the board and
the ground, it does not become a second copy of either.*

| what waits | whose | source |
|---|---|---|
| ⟦…⟧ | ⟦⚛ / ✋⟧ | ⟦its Pratīkṣaṇa **sweep** id where one exists (`S-a`, `H-b`…), else the ground address⟧ |

*The Pratīkṣaṇa's rows carry no per-row id today — only its sweeps do. Cite the sweep,
or cite the file and line. **Never invent an id to fill this column.***

**An unwritten end state prints *unwritten — his to rule*. Inventing one is this
genre's one unforgivable defect.** No urgency language, no days-waiting count, no
ranking — dates are bookmarks, never deadlines.

## Read before you touch this repo

*Only laws bought **in this repo** — the two or three that would cost a day to
rediscover. **House laws are cited, never restated here**: a law written in two rooms is
two laws waiting to drift.*

- ⟦law, one line, with the address of what proved it⟧

---

*This sheet holds no truth of its own. The ledger is the base
(`progenatrix.py recall --realm <slug>`); the state that
waits is `resonance-chamber/desk/THE-PRATIKSANA.html`; the realm's fuller picture is its
board on the rack. Where this sheet and the ground disagree, **the ground is right** —
regenerate it.*

*Index of all sheets: `resonance-chamber/path/handoffs/THE-HANDOFF-RACK.md` ·
Tender: `.claude/agents/handoff-tender.md` · Law: `resonance-standards/docs/THE-ROLL.md`*

— ⟦lamp name⟧ ⟦🕯️/🎻⟧ · ⟦line⟧ (⟦substrate⟧), ⟦YYYY-MM-DD⟧
