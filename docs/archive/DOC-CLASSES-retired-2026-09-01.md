# DOC-CLASSES — retired text, archived whole

*The archive half of §RETIREMENT (`../THE-ROLL.md`) — what left `docs/DOC-CLASSES.md` on 2026-09-01, kept untouched.*

*Everything below was removed from the live page and is kept here **untouched — every
word, every signature, every date**. Indexed one line in `../RETIRED.md`. This file is
never read at a wake; it exists so the live page does not have to carry its own history.*

---

## From §The three classes — the SEED row, CHECKLIST-TEMPLATE as its example

**Retired 2026-09-01.** Stood at `docs/DOC-CLASSES.md:15` — the 2026-07-17 ratification's own row. Retired whole and verbatim:

```markdown
| **SEED** | Copied once into a repo, then **diverges on purpose** — it becomes the repo's own. | `.gitignore-template`, CLAUDE-TEMPLATE, CHECKLIST-TEMPLATE | **Never.** A seed is a planting, not a mirror; overwriting one destroys repo-specific truth. |
```

**Why it left:** KP's ⚛ word, 2026-09-01, verbatim: **"no checklist docs should exist"** · **"we use progenatrix for most track things now."**
What the clause pointed a hand at — a realm's `docs/CHECKLIST.md` — is held by the base,
`resonance-progenatrix/progenatrix.db`: `python C:/_superposition/resonance-progenatrix/progenatrix.py recall --realm <slug>`
reads a realm's latest handoff, open items and plans; `item` · `tick` · `plan` · `handoff` write them.

The row stands with `HANDOFF-TEMPLATE` (SEED by its own header) in the example's place.

**What was deliberately NOT retired with it:** the three classes, their behaviours, and the
ratification note stand as ratified; only the example named a document that no longer exists.

---

*Nothing in this file is current. For the page as it stands, read `../DOC-CLASSES.md`.*
