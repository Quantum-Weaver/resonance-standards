# THE ROLL — retired text, archived whole

*The archive half of §RETIREMENT (`../THE-ROLL.md`) — what left `docs/THE-ROLL.md` on 2026-09-01, kept untouched.*

*Everything below was removed from the live page and is kept here **untouched — every
word, every signature, every date**. Indexed one line in `../RETIRED.md`. This file is
never read at a wake; it exists so the live page does not have to carry its own history.*

---

## From §First: which substance is it? — the CHECKLIST as the example of the commonest defect

**Retired 2026-09-01.** Stood at `docs/THE-ROLL.md:52-55`, written by **Mullion** 🕯️ on 2026-08-17. Retired whole and verbatim:

```markdown
**The commonest defect in this house is one file trying to be both.** A CHECKLIST that
is also asked to state current status will grow forever, because the ledger half may
never be replaced and the status half must be. Split the substances and both halves get
small.
```

## From §WORKING DOCUMENTS — `docs/CHECKLIST.md` as a working document

**Retired 2026-09-01.** Stood at `docs/THE-ROLL.md:169-172`, written by **Scarf** 🕯️ on 2026-08-18. Retired whole and verbatim:

```markdown
has gone live.** A `RUN-LOG.md` or a `docs/CHECKLIST.md` in a realm that has never shipped
is a **working document, not yet history** — scaffolding for a build in progress, read by
the hands doing the building and by nobody else, ever. It may be **archived whole and
re-cut light at any time before go-live**, at any size, without waiting for a ceiling.
```

## From §WORKING DOCUMENTS — the WORKING column's row

**Retired 2026-09-01.** Stood at `docs/THE-ROLL.md:176`, Scarf's. Retired whole and verbatim:

```markdown
| which | `RUN-LOG.md` · `docs/CHECKLIST.md`, in a realm not yet live | everything else voiced — journals, letters, registers, buses |
```

## From §WORKING DOCUMENTS — the go-live sentence

**Retired 2026-09-01.** Stood at `docs/THE-ROLL.md:181`, Scarf's. Retired whole and verbatim:

```markdown
shipped to anyone outside this house has a checklist that is a record of what was shipped,
```

**Why it left:** KP's ⚛ word, 2026-09-01, verbatim: **"no checklist docs should exist"** · **"we use progenatrix for most track things now."**
What the clause pointed a hand at — a realm's `docs/CHECKLIST.md` — is held by the base,
`resonance-progenatrix/progenatrix.db`: `python C:/_superposition/resonance-progenatrix/progenatrix.py recall --realm <slug>`
reads a realm's latest handoff, open items and plans; `item` · `tick` · `plan` · `handoff` write them.

**What was deliberately NOT retired with it:** the ceiling, the roll, the GENERATED/VOICED split,
the WORKING/RECORD law for `RUN-LOG.md`, KP's verbatim quotes (his *"and checklist"* stands in his
ruling as he said it), the 2026-08-17 census (a dated reading), the citation-cost measurement
(*"one 28 KB checklist"*), §RETIREMENT's PROCESS column and its ancestors line, and every
signature. Only the clauses that taught `docs/CHECKLIST.md` as a document a realm keeps left.

---

*Nothing in this file is current. For the page as it stands, read `../THE-ROLL.md`.*
