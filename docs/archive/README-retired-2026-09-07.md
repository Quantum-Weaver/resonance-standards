# README — retired text, archived whole

*The archive half of §RETIREMENT (`../THE-ROLL.md`) — what left `README.md` on 2026-09-07, kept untouched.*

*Everything below was removed from the live page and is kept here **untouched — every
word, every signature, every date**. Indexed one line in `../RETIRED.md`. This file is
never read at a wake; it exists so the live page does not have to carry its own history.*

---

## From §How To Use — the by-hand steps, and step 2, copy `CLAUDE.md` from the seed

**Retired 2026-09-07.** Stood at `README.md:109-121`. Retired whole and verbatim:

```markdown
In any new Sanctuary repo:

```powershell
# 1. Copy the gitignore
cp /path/to/resonance-standards/git/.gitignore-template .gitignore

# 2. Copy and customize CLAUDE.md
cp /path/to/resonance-standards/templates/CLAUDE-TEMPLATE.md CLAUDE.md

# 3. Tracking is the base's, not a file's — see where the realm stands
#    (KP's word, 2026-09-01: "no checklist docs should exist")
python ../resonance-progenatrix/progenatrix.py recall --realm <slug>
```
```

**Why it left:** KP's ⚛ word, 2026-09-07, verbatim: **"clear the stray claude md files, and lets update the founding ritual that is placing them"** · **"only truth of what exists."** The house holds one `CLAUDE.md`, the chamber's; no realm carries one, and the founding ritual plants none.

---

*Nothing in this file is current. For the page as it stands, read `../../README.md`.*
